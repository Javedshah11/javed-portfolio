import { motion, useReducedMotion } from 'framer-motion'
export default function LoadingScreen(){const reduce=useReducedMotion();return <motion.div initial={{opacity:1}} animate={{opacity:0}} transition={{delay:reduce?0:.7,duration:.35}} className="pointer-events-none fixed inset-0 z-[80] grid place-items-center bg-[#030609]"><div className="text-center"><motion.div initial={reduce?false:{clipPath:'inset(0 100% 0 0)'}} animate={{clipPath:'inset(0 0% 0 0)'}} transition={{duration:.55}} className="text-5xl font-black tracking-[-.08em] text-[#72FF37]">JS</motion.div><p className="mt-4 font-mono text-[10px] font-semibold tracking-[.24em] text-slate-400">INITIALIZING PORTFOLIO</p></div></motion.div>}
