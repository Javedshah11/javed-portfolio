import { useEffect, useState } from 'react'
import { navItems, profile } from '../../data/portfolio'
export default function Navbar({onCommand}) { const [open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false),[active,setActive]=useState('home'); useEffect(()=>{const scroll=()=>setScrolled(scrollY>30);const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.isIntersecting&&setActive(entry.target.id)),{rootMargin:'-35% 0px -55%'});navItems.forEach(({href})=>{const el=document.querySelector(href);if(el)observer.observe(el)});addEventListener('scroll',scroll,{passive:true});scroll();return()=>{removeEventListener('scroll',scroll);observer.disconnect()}},[]); useEffect(()=>{if(!open)return;const previous=document.body.style.overflow;document.body.style.overflow='hidden';const close=e=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',close);return()=>{document.body.style.overflow=previous;window.removeEventListener('keydown',close)}},[open]); return <header className={`nav-wrap ${scrolled?'scrolled':''}`}><nav aria-label="Primary navigation" className="nav"><a className="brand" href="#home" aria-label="Javed Shah home"><span>JS</span><b>Javed Shah</b></a><button className="menu-button" aria-label="Toggle navigation" aria-expanded={open} aria-controls="primary-menu" onClick={()=>setOpen(!open)}><i/><i/></button><div id="primary-menu" className={`nav-menu ${open?'open':''}`}>{navItems.map(({label,href})=><a className={active===href.slice(1)?'active':''} key={href} href={href} onClick={()=>setOpen(false)}>{label}</a>)}<div className="nav-social"><a href={profile.github} target="_blank" rel="noreferrer">GitHub</a><a href={profile.linkedin} target="_blank" rel="noreferrer">LinkedIn</a><a href="/Javed-Shah-CV.txt" target="_blank" download>Resume</a><button onClick={onCommand} aria-label="Open command palette">⌘K</button></div></div></nav></header> }
